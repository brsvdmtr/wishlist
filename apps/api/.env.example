DATABASE_URL=postgresql://wishlist:wishlist@localhost:5432/wishlist?schema=public
PORT=3001
WEB_ORIGIN=http://localhost:3000
ADMIN_KEY=dev_admin_key
# For POST /auth/telegram (Telegram WebApp initData validation). Same token as in bot.
BOT_TOKEN=your_bot_token_from_botfather
# Optional: for signing auth tokens (defaults to ADMIN_KEY)
# AUTH_SECRET=your_secret
SYSTEM_USER_EMAIL=owner@local
